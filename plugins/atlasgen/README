This tool automatically generates a texture atlas from a config with some other textures, also
converts it to config, that can be read by saddy using various tools.
It needs Ruby with Cairo and REXml installed. To install it, install rubygems.
You can install them, using gem install cairo and gem install rexml.
